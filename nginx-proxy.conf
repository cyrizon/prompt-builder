# Configuration Nginx pour Prompt Builder
# Ajoutez ce fichier à votre configuration Nginx existante

# Si vous utilisez un sous-domaine
server {
    listen 80;
    server_name prompt.yourdomain.com;  # Changez avec votre domaine

    location / {
        proxy_pass http://prompt-builder:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Ou si vous utilisez un sous-chemin sur votre domaine existant
# Exemple: yourdomain.com/prompt-builder
# server {
#     listen 80;
#     server_name yourdomain.com;
# 
#     location /prompt-builder {
#         rewrite ^/prompt-builder(.*) $1 break;
#         proxy_pass http://prompt-builder:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
